# Arlo UI pre-commit — fast, Turbo-cached checks that mirror the lint + test CI
# workflows. Instant when nothing relevant changed. Heavier build / e2e / drift
# checks run on pre-push (.husky/pre-push). Security scanning (Trivy) and audits
# run in CI (.github/workflows).
echo "▶ typecheck · lint · test"
npm run typecheck && npm run lint && npm test
